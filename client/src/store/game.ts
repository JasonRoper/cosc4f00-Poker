//import axios from 'axios'
//import GamePaths from '@/api/gameservice'
//import store from '@/store/index.js'

/*
export default class gamerequest {
  public userId: number

  constructor () {
    this.userId = parseInt(store.state.id)
  }

  public joinGame (): number {
      axios.post(GAME_PATHS.GAME_JOIN)
      .then(function (response) {

        
        //get confirmation 
        //and subscrite to a websocket through POKER Client


        //When you get a message back you will change the router and pass the values


        response.data.gameId
        //Switch Vue to TableVue
        router.push()//with params

        //Pass the GameId
        Gameservices.subscribe  - 

        console.log(response);
      })
      .catch(function (error) {
        console.log(error);
      });
  
    return this.hello
  }
  public set (newValue: string) {
    this.hello = newValue
  }
}
*/


